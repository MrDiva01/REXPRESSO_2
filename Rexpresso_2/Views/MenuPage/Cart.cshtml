@model List<Product>

<h2>Shopping Cart</h2>

@if (Model.Any())
{
    <ul>
        @foreach (var product in Model)
        {
            <li>@product.Name - @product.Price</li>
        }
    </ul>
}
else
{
    <p>Your shopping cart is empty.</p>
}